apiVersion: v1
kind: Service
metadata:
  name: {{ $.Release.Name }}-admin
  labels:
    app: {{ $.Chart.Name }}
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - port: 2019
    targetPort: 2019
    protocol: TCP
  selector:
    app: {{ $.Release.Name }}
